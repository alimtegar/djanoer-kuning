// Import Components
import { BackgroundLoader } from '../Loaders';

const HeroLoader = () => (
    <section className="position-relative d-flex justify-content-center align-items-center vh-75">
        <BackgroundLoader />
    </section>
);

export default HeroLoader;